(()=>{
  const css=`
  .l36-flow{display:flex;gap:7px;flex-wrap:wrap;align-items:center;margin-top:14px}.l36-flow span{background:#eef4fd;border:1px solid #d7e4f3;color:#315f96;border-radius:999px;padding:7px 9px;font-size:9px;font-weight:800}.l36-flow i{color:#8aa2bd}.l36-account-tabs{display:flex;gap:8px;overflow-x:auto;border-bottom:1px solid #dce7f2;margin:12px 0}.l36-account-tabs button{border:0;background:transparent;padding:10px 12px;font-weight:850;color:#71869e;cursor:pointer;border-bottom:3px solid transparent;white-space:nowrap}.l36-account-tabs button.active{color:#1d569e;border-bottom-color:#2f6fed}.l36-scorebox{display:grid;grid-template-columns:repeat(4,minmax(120px,1fr));gap:10px}.l36-scorebox .metric b{font-size:22px}.l36-primary-action{min-width:120px}.l36-account-name{cursor:pointer;color:#173e72}.l36-account-name:hover{text-decoration:underline}.l36-import-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:14px 0}@media(max-width:760px){.l36-scorebox,.l36-import-summary{grid-template-columns:1fr 1fr}}
  `;
  const style=document.createElement('style');style.textContent=css;document.head.appendChild(style);

  function money(v){const n=Number(v)||0;return n?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n):'—'}
  function renameNav(){document.querySelectorAll('.nav button').forEach(b=>{if(b.textContent.trim()==='Market Intelligence')b.textContent='Find Me Revenue';});}
  setTimeout(renameNav,50);
  const mo=new MutationObserver(renameNav);mo.observe(document.body,{childList:true,subtree:true});

  window.openFindMeRevenue=function(){show('Market Intelligence')};

  window.dashboard=function(){const p=state.portfolio?.products||[],b=state.portfolio?.brands||[],o=state.orgs||[];return `<div class="hero"><div><div class="eyebrow">RETAIL REVENUE INTELLIGENCE</div><h1>Find the next retailer, buyer and revenue opportunity.</h1><p>Start with your products. Launchpad36 identifies the best-fit accounts, competitive assortment, whitespace, buyer contacts and modeled manufacturer revenue in one workflow.</p><div class="l36-flow"><span>Products</span><i>→</i><span>Find Me Revenue</span><i>→</i><span>Account 360</span><i>→</i><span>Assortment Gap</span><i>→</i><span>Buyer</span><i>→</i><span>Opportunity</span></div></div><button class="btn l36-primary-action" onclick="openFindMeRevenue()">Find Me Revenue</button></div><div class="stats"><div class="card stat"><small>Brands</small><strong>${b.length}</strong><span class="muted">Private portfolio</span></div><div class="card stat"><small>Products</small><strong>${p.length}</strong><span class="muted">SKU-driven analysis</span></div><div class="card stat"><small>Searchable Accounts</small><strong>${o.length}</strong><span class="muted">Retail, distribution & dealer</span></div><div class="card stat"><small>Saved Opportunities</small><strong>${(state.workspaces||[]).length}</strong><span class="muted">Actionable workspaces</span></div></div><div class="grid2"><div class="card"><h3>Executive selling workflow</h3><p class="muted">Use Find Me Revenue to rank accounts, then open Account 360 to work assortment, buyers, opportunity and next action without jumping between modules.</p><button class="btn ghost" onclick="show('Accounts')">Open Account Universe</button></div><div class="callout"><b>Data confidence is separate from sales attractiveness</b><p class="muted">Fit measures account suitability. Gap measures assortment whitespace. Confidence measures evidence quality. Revenue is a modeled manufacturer opportunity.</p></div></div>`};

  window.accounts=function(){return `<div class="pageHead"><div><div class="eyebrow">ACCOUNT 360</div><h1>Account Universe</h1><p>Scan the accounts that matter, then open one workspace for assortment, buyers, opportunity and action.</p></div><div><button class="btn dark" onclick="openFindMeRevenue()">Find Me Revenue</button> <button id="retailerDiscoveryBtn" class="btn secondary" onclick="runRetailerDiscovery()">Discover New Retailers</button> <button class="btn secondary" onclick="downloadAccountTemplate()">Download Template</button> <button class="btn secondary" onclick="openAddAccount()">Add Account</button> <button class="btn" onclick="openAccountImport()">Import Accounts</button></div></div><div class="card"><div class="toolbar"><input id="acctQ" placeholder="Search account, retailer, distributor or domain"><select id="acctChannel"><option value="">All channels</option><option value="mass">Mass Retail</option><option value="ce">Consumer Electronics</option><option value="ecommerce">E-commerce</option><option value="distribution">Distribution</option><option value="specialty_av">Specialty AV</option><option value="automotive">Automotive</option><option value="convenience_travel">Convenience & Travel</option><option value="12v">12V / Automotive Specialty</option><option value="ci">CI / Custom Integration</option></select><button class="btn" onclick="searchAccounts()">Search Accounts</button></div><div id="accountResults">${accountTable(state.orgs)}</div></div>`};

  window.accountTable=function(rows){return `<div class="tableWrap"><table class="table"><thead><tr><th>Account</th><th>Channel</th><th>Head Office</th><th>Locations</th><th>Buyer</th><th>Title</th><th>Email</th><th>Phone</th><th>Fit</th><th>Gap</th><th>Confidence</th><th>Action</th></tr></thead><tbody>${(rows||[]).map(o=>{const buyer=(o.buyers||[])[0],fit=Number(o.fit_score)||0,gap=Number(o.whitespace_score)||0,conf=Number(o.confidence)||0;return `<tr><td><b class="l36-account-name" onclick="openAccount360('${esc(o.id)}')">${esc(o.name)}</b><div class="muted">${esc(o.organization_type||'retailer')} · ${esc(o.domain||'')}</div></td><td>${(o.channel_codes||[]).slice(0,2).map(c=>`<span class="badge">${esc(c.replaceAll('_',' '))}</span>`).join(' ')||'—'}</td><td>${esc(o.headquarters||o.region||'—')}</td><td>${Number(o.footprint)||'—'}</td><td>${buyer?esc(buyer.name):'—'}</td><td>${buyer?esc(buyer.title||buyer.category||'—'):'—'}</td><td>${buyer?.email?`<a href="mailto:${esc(buyer.email)}">${esc(buyer.email)}</a>`:'—'}</td><td>${buyer?.phone?`<a href="tel:${esc(buyer.phone)}">${esc(buyer.phone)}</a>`:'—'}</td><td><span class="score">${fit}</span></td><td><span class="score">${gap}</span></td><td>${conf}%</td><td><button class="btn dark" onclick="openAccount360('${esc(o.id)}')">Open Account</button></td></tr>`}).join('')}</tbody></table>${rows?.length?'':'<div class="empty"><b>No accounts loaded.</b><p>Import an account list or discover retailers to begin.</p></div>'}</div>`};

  async function load360Data(orgId){const org=(state.orgs||[]).find(x=>String(x.id)===String(orgId));if(!org)throw new Error('Account not found');const [buyerData,productData]=await Promise.all([api(`/api/buyers?organization_id=${encodeURIComponent(orgId)}`).catch(()=>({buyers:org.buyers||[]})),api(`/api/competitive-products?organization_id=${encodeURIComponent(orgId)}`).catch(()=>({products:[]}))]);const workspace=(state.workspaces||[]).find(w=>String(w.organization_id)===String(orgId));return {org,buyers:buyerData.buyers||[],products:productData.products||[],workspace};}

  window.openAccount360=async function(orgId,tab='overview'){const mc=$('modalCard');mc.className='modalCard accountResearchModal';mc.innerHTML='<div class="empty">Loading Account 360…</div>';$('modal').classList.add('show');try{const data=await load360Data(orgId);state.__account360=data;renderAccount360(tab)}catch(e){mc.innerHTML=`<h3>Account 360 could not load</h3><p class="muted">${esc(e.message)}</p><button class="btn secondary" onclick="closeModal()">Close</button>`}};

  window.renderAccount360=function(tab='overview'){const d=state.__account360;if(!d)return;const {org,buyers,products,workspace}=d;const fit=Number(org.fit_score)||0,gap=Number(org.whitespace_score)||0,conf=Number(org.confidence)||0;const scenario=workspace?.scenario||workspace?.result_snapshot||{},rev=scenario?.manufacturer_revenue||scenario?.revenue||workspace?.potential_revenue||0;const tabs=['overview','assortment','gap','buyers','opportunity','activity'];let body='';if(tab==='overview')body=`<div class="l36-scorebox"><div class="metric"><span class="label">Fit</span><b>${fit}</b><small class="muted">Account suitability</small></div><div class="metric"><span class="label">Gap</span><b>${gap}</b><small class="muted">Assortment whitespace</small></div><div class="metric"><span class="label">Data confidence</span><b>${conf}%</b><small class="muted">Evidence quality</small></div><div class="metric"><span class="label">Revenue potential</span><b>${money(rev)}</b><small class="muted">Modeled manufacturer revenue</small></div></div><div class="grid2" style="margin-top:14px"><div class="card"><h3>Account profile</h3><p><b>${esc(org.headquarters||org.region||'Head office not researched')}</b></p><p class="muted">${Number(org.footprint)||'—'} locations · ${(org.channel_codes||[]).map(x=>esc(x.replaceAll('_',' '))).join(', ')||'No channels saved'}</p><div class="workspaceActions"><button class="btn ghost" onclick="closeModal();openEditAccount('${esc(org.id)}')">Edit Account</button><button class="btn secondary" onclick="closeModal();openAccountResearch('${esc(org.id)}')">Refresh Intelligence</button></div></div><div class="card"><h3>Primary buyer</h3>${buyers[0]?`<b>${esc(buyers[0].name)}</b><p class="muted">${esc(buyers[0].title||buyers[0].category||'')}</p><p>${buyers[0].email?`<a href="mailto:${esc(buyers[0].email)}">${esc(buyers[0].email)}</a>`:'No email'}<br>${buyers[0].phone?`<a href="tel:${esc(buyers[0].phone)}">${esc(buyers[0].phone)}</a>`:'No phone'}</p>`:'<p class="muted">No buyer saved yet.</p>'}<button class="btn ghost" onclick="renderAccount360('buyers')">Open Buyers</button></div></div>`;
    if(tab==='assortment')body=products.length?productResearchTable(products.map(p=>({...p,name:p.product_name||p.name,purchase_channel:p.research_context?.purchase_channel||'UNKNOWN',store_verification:p.research_context?.store_verification||'NOT_REQUESTED'}))):'<div class="empty">No competitive assortment saved yet.<br><br><button class="btn" onclick="closeModal();openAccountResearch(\''+esc(org.id)+'\')">Research Account Products</button></div>';
    if(tab==='gap')body=`<div class="callout"><b>Assortment Gap Score: ${gap}/100</b><p class="muted">Gap is shown separately from Fit and Data Confidence so a seller can distinguish commercial whitespace from evidence quality.</p></div><div class="grid2" style="margin-top:12px"><div class="card"><h3>Current competitive set</h3><strong>${products.length}</strong><p class="muted">Saved comparable products</p></div><div class="card"><h3>Recommended next step</h3><p class="muted">Use Find Me Revenue with your selected SKUs to calculate the recommended mix and modeled account revenue.</p><button class="btn dark" onclick="closeModal();openFindMeRevenue()">Run Find Me Revenue</button></div></div>`;
    if(tab==='buyers')body=buyers.length?buyerTableHtml(buyers,true,true):`<div class="empty">No saved buyer contacts.<br><br><button class="btn" onclick="closeModal();openAccountResearch('${esc(org.id)}')">Research Buyers</button></div>`;
    if(tab==='opportunity')body=`<div class="l36-scorebox"><div class="metric"><span class="label">Fit</span><b>${fit}</b></div><div class="metric"><span class="label">Gap</span><b>${gap}</b></div><div class="metric"><span class="label">Confidence</span><b>${conf}%</b></div><div class="metric"><span class="label">Revenue</span><b>${money(rev)}</b></div></div><div class="card" style="margin-top:12px"><h3>${workspace?'Saved opportunity':'No saved opportunity yet'}</h3><p class="muted">${workspace?`Status: ${esc(workspace.status||'modeled')} · Priority: ${esc(workspace.priority||'medium')} · Next action: ${esc(workspace.next_action||'Not set')}`:'Run Find Me Revenue to model a SKU-level opportunity for this account.'}</p><div class="workspaceActions"><button class="btn dark" onclick="closeModal();openFindMeRevenue()">${workspace?'Recalculate Opportunity':'Create Opportunity'}</button><button class="btn secondary" onclick="closeModal();show('Opportunities')">Open Opportunities</button></div></div>`;
    if(tab==='activity')body=`<div class="card"><h3>Account actions</h3><p class="muted">Research, buyer updates, evidence review and opportunity changes remain source-tracked. Evidence approval is available when needed but is no longer the primary selling action.</p><div class="workspaceActions"><button class="btn secondary" onclick="closeModal();openAccountDataReview('${esc(org.id)}')">Review Evidence</button><button class="btn secondary" onclick="closeModal();openEditAccount('${esc(org.id)}')">Edit Account</button><button class="btn secondary" onclick="closeModal();openAccountResearch('${esc(org.id)}')">Refresh Intelligence</button></div></div>`;
    $('modalCard').innerHTML=`<div class="workspaceTop"><div><div class="eyebrow">ACCOUNT 360</div><h2>${esc(org.name)}</h2><p class="muted">One workspace for account profile, assortment, gap, buyers and opportunity.</p></div><button class="btn secondary" onclick="closeModal()">Close</button></div><div class="l36-account-tabs">${tabs.map(t=>`<button class="${t===tab?'active':''}" onclick="renderAccount360('${t}')">${({overview:'Overview',assortment:'Assortment',gap:'Gap Analysis',buyers:'Buyers',opportunity:'Opportunity',activity:'Activity'}[t])}</button>`).join('')}</div>${body}`;
  };

  const priorCommit=window.commitAccountImport;
  window.commitAccountImport=async function(sourceName){if(!pendingAccounts.length){toast('Preview accounts first');return}try{const d=await api('/api/retail-universe-import',{method:'POST',body:JSON.stringify({source_name:sourceName,rows:pendingAccounts})});await load();const buyers=Number(d.buyers_imported)||0,errors=(d.errors||[]).length;$('modalCard').innerHTML=`<div class="eyebrow">IMPORT COMPLETE</div><h2>Account data saved</h2><div class="l36-import-summary"><div class="metric"><span class="label">Accounts</span><b>${Number(d.rows_imported)||0}</b></div><div class="metric"><span class="label">Buyers</span><b>${buyers}</b></div><div class="metric"><span class="label">Errors</span><b>${errors}</b></div></div><p class="muted">Existing accounts were updated in place where names matched; new accounts were added without duplicating the account universe.</p><div class="workspaceActions"><button class="btn dark" onclick="closeModal();show('Accounts')">View Accounts</button><button class="btn secondary" onclick="closeModal()">Close</button></div>`;}catch(e){toast(e.message)}};

  window.__L36_EXECUTIVE_WORKFLOW_UI__=true;
})();