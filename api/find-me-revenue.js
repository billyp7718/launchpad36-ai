import { db } from './_db.js';
import { resolveTenant } from './_tenant.js';
import { evaluateProductAccountFit, evidenceProfiles } from './_account-fit.js';
export default async function handler(req,res){const t=await resolveTenant(req,res);if(!t)return;if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const sql=db();try{const productId=req.body?.product_id||null;let product=null;if(productId)product=(await sql`select p.*,coalesce((select array_agg(category) from product_categories pc where pc.product_id=p.id),'{}') categories,coalesce((select array_agg(c.code) from product_channels pc join channels c on c.id=pc.channel_id where pc.product_id=p.id),'{}') channels from products p where p.id=${productId} and p.manufacturer_id=${t.tenant_id} limit 1`)[0];if(productId&&!product)return res.status(404).json({error:'Product not found for this tenant'});if(!product)return res.status(400).json({error:'A tenant-owned product is required'});const [orgs,verifiedEvidence]=await Promise.all([sql`select * from retail_organizations where active=true limit 5000`,sql`select ce.organization_id,cct.payload,cct.source_url,cct.observed_at,cct.last_verified_at,cct.confidence from current_commercial_truth cct join commercial_evidence ce on ce.id=cct.evidence_id where ce.organization_id is not null and cct.subject_type='retailer_assortment' and cct.verification_status='VERIFIED'`]),profiles=evidenceProfiles(verifiedEvidence);const ranked=orgs.map(o=>{const fit=evaluateProductAccountFit(product,o,profiles.get(String(o.id))||null);if(!fit.qualified)return null;return {organization_id:o.id,name:o.name,domain:o.domain,channels:o.channel_codes,categories:o.categories,coverage:o.coverage,verification_status:o.verification_status,confidence:Number(o.confidence)||0,fit_score:fit.score,fit_tier:fit.tier,fit_reason:fit.reason,evidence_status:fit.evidence_status,evidence_count:fit.evidence_count||0,last_verified_at:fit.last_verified_at||null}}).filter(Boolean).sort((a,b)=>(b.evidence_status==='VERIFIED')-(a.evidence_status==='VERIFIED')||b.fit_score-a.fit_score).slice(0,Math.min(Number(req.body?.limit)||100,250));return res.status(200).json({version:'9.8.3',product:{id:product.id,name:product.name},criteria:{categories:product.categories||[],channels:product.channels||[]},opportunities:ranked,excluded_unqualified_count:orgs.length-ranked.length,interpretation:'Accounts qualify only through a relevant category profile or verified assortment evidence. Company size and confidence cannot qualify an unrelated account.'})}catch(e){console.error('find revenue failed',{message:e?.message||String(e)});return res.status(500).json({error:'Account ranking could not be completed'})}}
