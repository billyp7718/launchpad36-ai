import { db } from './_db.js';
import { requireInternal } from './_auth.js';
export default async function handler(req,res){if(!requireInternal(req,res))return;if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const sql=db(),limit=Math.min(Math.max(Number(req.query?.limit)||100,1),500);const rows=await sql`with latest as(select distinct on(ce.subject_type,ce.subject_key) ce.*,es.source_url from commercial_evidence ce join evidence_sources es on es.id=ce.source_id order by ce.subject_type,ce.subject_key,ce.observed_at desc) select subject_type,subject_key,source_url,observed_at,last_verified_at as last_verified,confidence,evidence_type,verification_status,case when jsonb_typeof(payload->'offerings')='array' then jsonb_array_length(payload->'offerings') else 0 end as offering_count,exists(select 1 from intelligence_change_events ice where ice.current_evidence_id=latest.id and ice.meaningful=true) as changed_since_last_check from latest order by observed_at desc limit ${limit}`;const pending=await sql`select count(*)::int count from intelligence_change_events where verification_status in ('REVIEW_REQUIRED','CONFLICTING','UNKNOWN')`;return res.status(200).json({version:'9.8.3',operational_status:rows,pending_review:pending[0]?.count||0,fields:['Last Verified','Confidence','Evidence Source','Verification Status','Changed Since Last Check','Structured Products']})}catch(e){return res.status(500).json({error:e.message})}}
