import { db, upsertBuyer } from './_db.js';
import { requireInternal } from './_auth.js';
import { persistEvidence } from './_evidence.js';
import { runLivingIntelligencePipeline } from './_living-intelligence.js';
import { resolveTenant } from './_tenant.js';
function origin(req){const proto=req.headers['x-forwarded-proto']||'https';const host=req.headers['x-forwarded-host']||req.headers.host;return `${proto}://${host}`}
function normalizeTitle(t=''){return String(t).replace(/\s+/g,' ').trim()}
function roleScore(title=''){const t=title.toLowerCase();if(/chief merchant|chief merchandising/.test(t))return 95;if(/vice president|vp/.test(t)&&/merch|category|buy|retail/.test(t))return 90;if(/director/.test(t)&&/merch|category|buy/.test(t))return 85;if(/senior buyer|category manager|merchant/.test(t))return 82;if(/\bbuyer\b/.test(t))return 78;if(/merch|category/.test(t))return 65;return 35}
function status(conf,sourceCount,ageDays){if(conf>=85&&sourceCount>=2&&ageDays<=180)return 'Verified Current';if(conf>=70&&ageDays<=365)return 'Likely Decision Maker';if(sourceCount>=1)return 'Publicly Listed';return 'Stale / Unverified'}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!requireInternal(req,res))return;const tenant=await resolveTenant(req,res,{allowCron:true});if(!tenant)return;const sql=db(),accountId=req.body?.account_id,manufacturerId=tenant.tenant_id||req.body?.manufacturer_id||null;if(!accountId)return res.status(400).json({error:'account_id is required'});try{const account=(await sql`select * from accounts where id=${accountId}`)[0];if(!account)return res.status(404).json({error:'Account not found'});const headers={};if(req.headers.authorization)headers.authorization=req.headers.authorization;if(req.headers.cookie)headers.cookie=req.headers.cookie;const r=await fetch(`${origin(req)}/api/decision-makers?domain=${encodeURIComponent(account.domain||'')}&account=${encodeURIComponent(account.name)}`,{headers});if(!r.ok){const detail=await r.text();return res.status(r.status===401?500:502).json({error:r.status===401?'BUYER_DISCOVERY_AUTH_FAILED':'decision-maker discovery failed',upstream_status:r.status,detail:detail.slice(0,300)})}const data=await r.json(),now=Date.now(),results=[];for(const p of(data.people||[])){if(!p.name||!p.title||!p.source_url)continue;const verifiedAt=p.verified_at||new Date().toISOString();const ageDays=Math.max(0,Math.floor((now-new Date(verifiedAt).getTime())/86400000));const sources=[p.source_url].filter(Boolean);const rs=roleScore(p.title);const confidence=Math.min(95,Math.round((Number(p.confidence)||60)*.65+rs*.35));const buyerStatus=status(confidence,sources.length,ageDays),verificationStatus=confidence>=85?'VERIFIED':'REVIEW_REQUIRED';const payload={...p,role_score:rs,buyer_status:buyerStatus};const living=await runLivingIntelligencePipeline({account_id:account.id,source_url:p.source_url,source_type:'public_professional_source',domain:account.domain,subject_type:'buyer',subject_key:`${account.id}:${p.name}|${p.title}`,evidence_type:'buyer',payload,observed_at:verifiedAt,last_verified_at:verifiedAt,confidence,verification_status:verificationStatus,acquired_by:'buyer_intelligence'});let record=null;if(living.deterministic_update_applied)record=await upsertBuyer({account_id:account.id,name:p.name,title:normalizeTitle(p.title),email:p.email||'',phone:p.phone||'',linkedin:p.linkedin||'',category:p.category||account.category||'',source:p.source_label||'Public business source',source_url:p.source_url,confidence,verified_at:verifiedAt,status:buyerStatus,notes:`L36 buyer intelligence role_score=${rs}`,observed_at:verifiedAt,last_verified_at:verifiedAt,evidence_type:'buyer',verification_status:'VERIFIED'});const evidence=await persistEvidence({manufacturer_id:manufacturerId,account_id:account.id,evidence_type:'buyer',entity_key:`${p.name}|${p.title}`,payload,source_url:p.source_url,source_type:'public_professional_source',observed_at:verifiedAt,confidence});results.push({buyer_id:record?.id||null,name:p.name,title:p.title,status:buyerStatus,verification_status:living.verification_status,changed_since_last_check:living.changed_since_last_check,confidence,role_score:rs,source_url:p.source_url,last_verified:living.last_verified_at,evidence_id:evidence?.id||null,linkedin:p.linkedin||''})}return res.status(200).json({version:'9.8.3',account:{id:account.id,name:account.name,domain:account.domain},buyer_intelligence:results.sort((a,b)=>b.confidence-a.confidence),linkedin_policy:{mode:'verification_enrichment_only',bulk_scraping:false,private_contact_inference:false},interpretation:results.length?'Candidate decision-makers were found from attributable public sources; unverified candidates remain in review.':'No attributable named buyer candidates were found. This does not prove no relevant buyer exists.'})}catch(e){return res.status(500).json({error:e.message})}}
